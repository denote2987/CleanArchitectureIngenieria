# Database Configuration
# =====================

# Option 1: Full connection string (takes priority over individual settings)
# Uncomment and set if you want to use a complete connection string instead of individual variables
# DB_CONNECTION_STRING=Data Source=localhost;Initial Catalog=InventarioDb;User Id=sa;Password=YourPassword;

# Option 2: Individual database settings
# Server/Host
DB_SERVER=localhost

# Database name
DB_DATABASE=InventarioDb

# Use Windows Authentication (Integrated Security)
# Set to 'true' for Integrated Security or 'false' for SQL Server authentication
DB_USE_INTEGRATED_SECURITY=true

# SQL Server username (only needed if DB_USE_INTEGRATED_SECURITY=false)
# DB_USER_ID=sa

# SQL Server password (only needed if DB_USE_INTEGRATED_SECURITY=false)
# DB_PASSWORD=YourPassword

# Connection timeout in seconds (default: 30)
DB_CONNECT_TIMEOUT=30

# Application Environment
ASPNETCORE_ENVIRONMENT=Development

# Logging level
ASPNETCORE_LOGGING_LEVEL=Information
